# The site build (src/tools/build_site.py, site_instances.py). Pinned so the pages are reproducible.
# pycountry: the Schemas page runs src/tools/qa.py, whose glossary-matches check recomputes ISO 3166 alpha-3 codes.
markdown==3.8.1
pycountry==24.6.1
